# Copy to ~/.config/autohotspot.env and edit values. Do NOT commit real values.

# Connection name for NetworkManager
HOTSPOT_CON=dotfiles-hotspot

# SSID and WPA2 PSK for the AP
HOTSPOT_SSID=my-arch-hotspot
HOTSPOT_PSK=changeme-please

# Optional: force Wi-Fi/Ethernet interface names (leave empty to auto-detect)
# HOTSPOT_WIFI_IF=wlp0s20f3
# HOTSPOT_WIRE_IF=enp3s0

# band: bg (2.4GHz) or a (5GHz). channel: auto or number.
HOTSPOT_BAND=bg
HOTSPOT_CHANNEL=auto

# Autostart policy for nm-hotspot-autostart: ethernet|always|never
HOTSPOT_AUTOSTART_POLICY=ethernet

