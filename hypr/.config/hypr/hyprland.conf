# Minimal Hyprland config (edit as needed)

monitor=,preferred,auto,1

input {
  kb_layout=us
}

general {
  gaps_in=5
  gaps_out=10
}

bind=SUPER,Return,exec,kitty
bind=SUPER,Q,killactive,

# Include host-specific overrides if present
source=~/.config/hypr/host.conf
